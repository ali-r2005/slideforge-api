services:
  - type: web
    name: slideforge-api
    env: python
    plan: standard
    buildCommand: bash build.sh
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.10
      - fromGroup: slideforge-api-env
